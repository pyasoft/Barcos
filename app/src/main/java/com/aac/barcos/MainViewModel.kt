package com.aac.barcos

import androidx.lifecycle.ViewModel

class MainViewModel: ViewModel() {
/*
    // state: todoItems
    private var _todoItems = MutableLiveData(listOf<Model.NauticaComun>())
    val todoItems: LiveData<List<Model.NauticaComun>> = _todoItems

    // event: addItem
    fun addItem(item: Model.NauticaComun) {
        // some imaginary backend call

    }

    fun getDetailText(): String {
        // some imaginary backend call
        return "Detail"
    }

*/
}